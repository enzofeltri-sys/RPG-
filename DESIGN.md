# Le Sceau de Vaeloria — Design de référence

Document vivant : mis à jour à chaque incrément. Sert de mémoire du projet entre les sessions.

Portée : ce document couvre la v1 (voir "Portée v1" ci-dessous) et sa roadmap d'implémentation par incréments. Pour la vision long terme du jeu dans son ambition complète (au-delà de la v1), voir `VISION.md`.

## Décisions techniques

- Moteur : Phaser 3, TypeScript, Vite
- Vue : top-down 2D classique (façon Zelda/Pokémon), pas isométrique
- Style graphique : rétro 16-bit façon SNES (résolution interne 216×384 portrait, `pixelArt: true`)
- UI/texte : tout texte passe par `addCrispText` (`src/ui/text.ts`) — `pixelArt: true` applique un filtrage "nearest" à tout, y compris le texte, ce qui le rend flou/blocky une fois agrandi sur mobile ; `addCrispText` force une résolution de rendu plus haute + filtrage linéaire pour ce texte spécifiquement. Ne jamais utiliser `this.add.text(...)` directement pour de l'UI. Police : VT323 (pixel/terminal, OFL, auto-hébergée dans `src/assets/fonts/` + `src/fonts.css`, jamais de CDN Google Fonts — casserait le hors-ligne) remplace la police serif d'origine (Georgia), illisible une fois réduite à la résolution interne ; `addCrispText` applique aussi un multiplicateur de taille (`FONT_SIZE_MULTIPLIER`) car les glyphes VT323 paraissent plus petits que Georgia à taille égale. `main.ts` attend `document.fonts.load(...)` avant de démarrer `Phaser.Game`, sinon le premier rendu du texte peut figer sur la police de repli (Phaser ne redessine pas automatiquement le texte quand une police web finit de charger après coup).
- Contrôles tactiles standards par scène de jeu : joystick virtuel (bas gauche), bouton "Action" (bas droite, interaction contextuelle), bouton "Menu" (haut gauche, fiche personnage + inventaire + options + quitter). Les boutons interactifs ne doivent jamais être des enfants d'un `Phaser.GameObjects.Container` — la détection de clic dessus est peu fiable ; les garder au niveau scène et gérer leur visibilité manuellement en parallèle du container visuel. Piège vécu : un objet `Text` retiré du container pour cette raison doit récupérer manuellement `setScrollFactor(0)` et un `setDepth` au-dessus du fond du panneau — sinon il reste ancré au monde (défile avec la caméra) et/ou se retrouve rendu sous le fond opaque.
- Menu → Options : bouton "Vider le cache et rafraîchir" (désenregistre le service worker + vide le Cache Storage + recharge la page) pour les cas où une mise à jour PWA ne s'applique pas toute seule.
- Combat : tour par tour (menu d'actions), écran de combat dédié
- Contrôles : joystick virtuel tactile + boutons d'action (priorité tactile, mobile en **portrait**, quasi plein écran via CSS `100dvh` + bouton plein écran natif où disponible), clavier flèches en support secondaire pour les tests
- Hors-ligne : PWA (service worker via `vite-plugin-pwa`), sauvegarde locale IndexedDB
- Hébergement de test : GitHub Pages (déploiement auto sur push via `.github/workflows/deploy.yml`), à l'URL `https://enzofeltri-sys.github.io/RPG-/`

## Scénario (pitch validé)

Il y a 300 ans, une confrérie de mages a scellé une entité primordiale — **le Roi Démon** — au prix de leur vie. Le sceau a fracturé les royaumes unifiés en cités-États rivales et dispersé des éclats de corruption qui pervertissent peu à peu faune, flore et parfois hommes.

Ton : sérieux pour la trame principale, pas de comique. Conflits politiques entre cités, ambiguïté morale de certaines factions et de l'antagoniste. Le registre "drôle et étrange" façon Daedra (voir `VISION.md`) est réservé aux quêtes secondaires, en contraste volontaire avec le sérieux de la trame principale.

### Trame principale (3 actes)

**Acte 1 — L'Éveil de la Marque**
Le héros/l'héroïne, originaire d'un village frontalier, survit à une attaque de créature corrompue et découvre une marque liée au rituel de scellement originel. Un aîné du village (mentor) la reconnaît et pousse le héros à enquêter — première quête dans un site rituel voisin corrompu (1er donjon). Fin d'acte : la corruption ne se répand pas au hasard, quelqu'un arrache activement des éclats du sceau quelque part dans les cités.

**Acte 2 — Les Cités Rivales**
Monde ouvert guidé : le héros voyage entre plusieurs cités-États, chacune dominée par une des 3 factions (voir "Factions" ci-dessous). Le héros apprend que sa marque lui permet de s'accorder aux éclats du sceau (les sentir, les extraire, les renforcer) — il/elle devient une cible pour ceux qui veulent protéger le sceau *et* pour ceux qui veulent le briser. Fin d'acte : un donjon majeur (tombeau de la confrérie fondatrice) est pillé par l'antagoniste, qui vole un éclat majeur — une région entière tombe en corruption.

**Acte 3 — Rupture ou Rédemption**
Poursuite de l'antagoniste jusqu'au site originel du scellement (donjon final, version corrompue/miroir d'un lieu déjà visité). Révélation : la confrérie originelle n'a jamais vaincu le Roi Démon, elle n'a fait que le contenir, et le sceau était condamné à s'affaiblir depuis le début — la marque du héros est un fragment de son essence, lié par le sang à la lignée d'un des mages fondateurs (le héros en est un descendant). L'antagoniste n'est pas un simple méchant caricatural : convaincu que le confinement est voué à l'échec, il/elle veut soit détruire le Roi Démon pour de bon, soit absorber son pouvoir avant que quelqu'un de pire ne le fasse.

**Fins multiples**, déterminées par les alliances de faction construites en acte 2 :
1. **Nouveau Sceau** — le héros se lie lui-même comme ancre vivante : le monde est stabilisé, à un prix personnel (sacrifice, fin douce-amère).
2. **Destruction** — avec l'aide des factions alliées, destruction complète du Roi Démon (fin "victoire", avec pertes selon les alliances ratées).
3. **Corruption/Ascension** — le héros absorbe le pouvoir du Roi Démon et devient une nouvelle puissance dominante sur les royaumes fracturés (fin sombre/ambiguë).

### Quêtes secondaires — répartition

- **Acte 1** : quêtes locales au village de départ et alentours (ambiance, premiers PNJ excentriques).
- **Acte 2** (le plus long) : gros de la production de quêtes secondaires et de faction, dispersées librement dans les cités/camps/donjons annexes, pas bloquées derrière la trame principale.
- **Acte 3** : peu de nouvelles quêtes secondaires (rythme resserré vers le climax) ; certaines quêtes de l'acte 2 peuvent s'y conclure selon les choix faits.
- **Post-jeu** : lot dédié de quêtes secondaires débloquées après la fin de la trame principale — contenu supplémentaire jouable après le générique, terrain privilégié pour les quêtes les plus étranges/Daedra-like.

### Factions

Poids volontairement simple (même philosophie que l'artisanat — pas de micro-gestion) :
- **Réputation par faction** → accès à des marchands/objets/recettes exclusifs à cette faction + tarifs préférentiels.
- **Quêtes de faction** → chaînes courtes dédiées, récompensent de l'équipement unique à la faction.
- **Poids sur la fin** → l'aide disponible en acte 3 (qui accompagne le héros dans l'affrontement final, quelle fin est réellement accessible) dépend des factions aidées en acte 2. Pas de jauge complexe : juste "aidée ou pas", qui ouvre/ferme des portes plus tard.

Autres éléments actés :
- Donjons = anciens sites rituels/tombeaux contenant des artefacts uniques
- Artisanat = redécouverte de techniques perdues depuis la Rupture
- Races : Humains (royaumes fracturés) et Elfes (gardiens reclus) au lancement ; Nains (mineurs souterrains) et Orcs (exilés en rédemption) en extension

## Portée v1 (première version jouable)

- ✅ 3-4 villes/villages reliés par une zone extérieure explorable (dépassé : 20 lieux au total)
- ✅ 4-6 donjons courts à moyens (Repaire du Loup, Catacombes, Entrepôt, Tanière des marais + Grotte/Vieux puits/Archives en détours plus légers)
- ✅ 2 classes × 2 races au lancement (Guerrier/Mage × Humain/Elfe) — extension à Voleur/Clerc et Nain/Orc laissée pour une v2
- ✅ 1 quête principale + 2-4 quêtes secondaires par ville/donjon — la trame principale s'est étendue bien au-delà des 5-8 étapes visées à l'origine (14 étapes) et se conclut désormais sur une vraie fin d'Acte 1 (`act1_complete`) plutôt que de s'arrêter au milieu ; 14 quêtes secondaires réparties sur 8 lieux
- ✅ Équipement : tous les emplacements dès la v1, rareté commun → épique (dépassé la cible initiale rare)
- ✅ Artisanat de base (dépassé : 5 recettes, forge + alchimie, tiers commun/rare de matériaux)
- ✅ 9 coffres à butin garanti répartis dans les lieux isolés/donjons
- 🟡 Effets sonores procéduraux (combat, coffres, boutique, artisanat, récompenses de quête) — pas de vrais graphismes/musique, laissés pour une v2 (voir incrément 10)

**v1 est fonctionnellement complète.** Il ne reste que du polish visuel/sonore avancé (vrais sprites, musique) pour l'incrément 10, volontairement hors scope v1 (nécessite de vrais assets graphiques).

## Règles de loot (à appliquer dès l'implémentation du système d'objets)

- **Objets uniques** (rareté la plus haute, ~0,10% de drop) : répartis sur 10 à 20 donjons/quêtes difficiles au total sur tout le jeu. **Tous les donjons n'en offrent pas** — chaque donjon/quête est explicitement tagué "a une table de loot unique" ou non lors de sa conception ; ne pas en distribuer uniformément.
- **Puissance des objets liée à la progression de l'histoire** : les tables de loot sont segmentées par palier narratif (chapitre/région), pas seulement par difficulté du donjon. Un objet légendaire trouvé tôt dans l'histoire doit être significativement moins puissant qu'un équivalent trouvé dans les zones tardives — la montée en puissance suit l'avancement du scénario, en plus de la rareté.
- Rareté : commun → rare → épique → légendaire → unique, avec effets spéciaux sur les objets rares et au-dessus.

## Roadmap par incréments

1. ✅ Setup projet (Phaser3+TS+Vite+PWA), écran titre, sauvegarde/chargement
2. ✅ Overworld : déplacement (joystick tactile + flèches), 1re map (village de départ, formes géométriques en attendant les vrais tilesets), collisions, transition de zone avec fondu
3. ✅ Combat tour par tour (menu Attaquer/Fuir, 1 monstre test — loup corrompu), rencontres aléatoires en explorant le Champ, XP/niveau désormais fonctionnels (gain d'XP, montée de niveau avec points de stat, restauration PV/PM)
4. ✅ Création de personnage : choix race/classe (Humain/Elfe × Guerrier/Mage), stats dérivées, PV/PM, fiche consultable en jeu. XP/niveaux/points de compétence : structure de données en place (niveau 1, XP 0), sans encore de combat pour les faire progresser (arrive à l'incrément 3)
5. ✅ Inventaire + équipement complet (10 emplacements, stats variables, rareté commun/rare), butin de combat, stats effectives (base + équipement) utilisées en combat. Bouton "Menu" (fiche personnage/inventaire/quitter) disponible à la fois au Village et au Champ. Chaque objet peut être consulté en détail (stats complètes, et pour les objets non équipés une comparaison "avant → après" contre l'objet actuellement dans le même emplacement) avant de confirmer l'équipement/déséquipement. Objets rares : gagnent en plus un effet spécial fixe propre au gabarit d'objet (ex. "Dégâts de feu" sur l'épée courte), au-delà du simple bonus de statistique ×2 — Armure ajoutée comme statistique d'objet standard (boucliers/armures), utilisée en combat pour réduire les dégâts subis ; Dégâts de feu ajouté comme exemple d'effet spécial d'arme, ajouté aux dégâts infligés.
6. ✅ 1er donjon jouable avec butin + boss — "Repaire du Loup", accessible depuis le Champ (zone du haut). 2 combats réguliers (rat des cavernes, loup corrompu) dans un couloir central, puis une barrière physique bloque l'accès au boss (loup alpha corrompu, 70 PV) tant que les deux ne sont pas déclenchés — empêche de foncer direct sur le boss en contournant les rencontres. Butin garanti sur le boss (`rollLootItem({ guaranteed: true, rareChance: 0.5 })`), butin normal (40% de chance) sur les combats réguliers.
7. ✅ Système de quêtes + PNJ/dialogues — mécanisme minimal volontairement (voir note sous l'incrément 9) : `src/game/quest.ts` (`QUESTS` registry, accepter/suivre/terminer, objectif "vaincre N fois tel monstre"), persistance dans `character.quests`. Un PNJ (villageois, près du premier bâtiment) donne la seule quête actuelle ("Menace dans les bois" — vaincre 3 loups corrompus, au Champ ou dans le donjon) via une boîte de dialogue (fond + texte + boutons ; suit la même règle que le Menu : boutons hors `Container`). `CombatScene.victory()` fait avancer toute quête active ciblant le monstre vaincu et le signale dans le journal de combat. Écran "Quêtes" dédié (liste titres/description/statut) accessible depuis le Menu (bouton "Quêtes", grille 2×2 avec Inventaire/Options/Quitter).
8. ✅ Artisanat de base + économie (or, marchand) — `src/game/material.ts` (fer brut, herbe), `src/game/consumable.ts` (potion de soin, `useConsumable`), `src/game/recipe.ts` (`RECIPES` : forge épée courte 3 fer brut, alchimie potion de soin 2 herbes ; `canCraft`/`craft` déduisent les matériaux et ajoutent l'objet/consommable). Récolte : 2 nœuds au Village (gisement de fer, herbes sauvages), bouton Action → `+1 matériau`. Marchande (PNJ dédié au Village) : `MerchantScene` avec vente des objets non équipés de l'inventaire (`sellPrice`, rareté : commun 10 or, rare 25 or) et achat d'un petit catalogue (fer brut, herbe, potion de soin) contre `character.gold`. Forge (bâtiment dédié) : `CraftingScene` liste les recettes par station, affiche matériaux possédés/requis (vert si suffisant), bouton "Fabriquer" désactivé sinon. Combat : chaque victoire rapporte de l'or (`monster.goldReward`) en plus de l'XP/butin ; `CombatScene` propose un bouton "Potion de soin" en combat si le joueur en possède. Fiche personnage : ligne "Or" ajoutée aux stats affichées.
**Retours de playtest post-incrément 8 (corrigés)** : le Menu (Inventaire/Sac/Stats/Quêtes) ramenait systématiquement au point de spawn de la scène au lieu de la position exacte du joueur — pire, depuis le Champ ou le Donjon, il ramenait carrément au Village. Cause : `InventoryScene`/`QuestLogScene` faisaient `this.scene.start('Village')` en dur. Fix : `src/ui/returnContext.ts` (`ReturnSceneKey`, `returnSceneStartData`) fait transiter `{ returnScene, x, y }` à travers toute navigation partant du Menu ou des bâtiments du Village (forge/marchande) ; chaque scène (`Village`/`Field`/`Dungeon`) accepte `x`/`y` en `init()` pour spawn au bon endroit, et le retour vers le Donjon renvoie systématiquement `resume: true` pour ne pas effacer la progression des rencontres. Par la même occasion : la fiche personnage (stats/or/PV/PM/points) a été sortie du panneau Menu vers un écran dédié `StatsScene` ("Stats") ; l'inventaire a été scindé en `InventoryScene` ("Inventaire", emplacements équipés uniquement) et `BagScene` ("Sac", objets non équipés — équiper ou jeter, avec confirmation en deux temps pour le jet). Menu réorganisé en grille 3×2 : Inventaire/Sac, Stats/Quêtes, Options/Quitter. Le Sac a ensuite reçu un filtre à 4 onglets (Objets/Ress./Potions/Quête) pour regrouper tout l'inventaire non-équipement au même endroit : objets non équipés (équiper/jeter, inchangé), ressources d'artisanat (`character.materials`, lecture seule), consommables (`character.consumables`, bouton "Utiliser" direct — soigne immédiatement hors combat), et objets de quête (`src/game/questItem.ts`, `character.questItems` — lecture seule, aucune quête n'en distribue encore ; prêt pour le contenu narratif de l'incrément 9). L'onglet Objets a ensuite été transformé en grille d'icônes carrées (4 colonnes) au lieu d'une liste de noms — chaque case est un badge coloré par rareté (`RARITY_COLORS`) avec un code catégorie à 3 lettres (`categoryIcon()` dans `src/game/item.ts` — vrais visuels prévus à l'incrément 10). Un objet plus puissant que celui actuellement équipé dans son emplacement (`isUpgrade()`, comparaison par somme de stats — la rareté y est déjà intégrée puisqu'elle multiplie les stats) reçoit une bordure épaisse + halo vert sur sa case, et le détail de l'objet affiche "▲ Plus puissant que l'objet équipé" (vert) ou "▼ Moins puissant" (gris) au-dessus du comparatif stat-par-stat déjà existant (`compareItemStats`).
9. ✅ Extension du contenu v1 (villes/donjons/quêtes restants) — c'est ici qu'arrive le vrai contenu narratif (histoire, quêtes écrites, dialogues) une fois les incréments 6-8 posés ; jusque-là le système de quêtes (incrément 7) restait volontairement un mécanisme technique minimal, pas du contenu. Clos avec la fin d'Acte 1 de la trame principale (voir plus bas) — v1 est désormais fonctionnellement complète.

**Refonte du monde (première passe increment 9)** : jusqu'ici le "Village" cumulait tout (mentor/quête, marchande, forge, récolte) dès la toute première seconde de jeu, et le Champ n'était qu'un couloir vide entre deux zones. Découpage en 3 lieux distincts pour un monde partiellement ouvert, cohérent avec le pitch (survivant d'un village frontalier) :
- **Basse-Combe** (`HamletScene`, nouvelle, clé `Hamlet`) — le hameau de départ, volontairement pauvre : 2 cabanes décoratives + le mentor ("Aldric", ex-"Villageois") qui donne la quête `wolves_threat`. C'est le point d'entrée pour une nouvelle partie et pour "Continuer" (`TitleScene`/`CharacterCreationScene` démarrent sur `Hamlet`, pas `Village`).
- **Le Champ** (`FieldScene`, agrandi 320×240 → 480×480) — désormais un vrai carrefour rempli : une rivière traverse la carte (colliders des deux côtés, seul le pont — sans collider — permet de passer), arbres/rochers décoratifs (aucune collision, pas de vrais assets avant l'incrément 10), les 2 nœuds de récolte (déplacés depuis le Village), et 3 sorties (hameau au sud, Repaire du Loup au nord — de l'autre côté de la rivière —, Forêt à l'est). Bouton "Action" ajouté pour la récolte (le Champ n'en avait pas besoin avant).
- **Valombre** (`VillageScene`, clé `Village` inchangée) — la vraie ville, avec la marchande et la forge ; le PNJ de quête et les nœuds de récolte en sont partis (vers Basse-Combe / le Champ).

Position du joueur préservée à travers toutes ces transitions (spawn au bon endroit selon d'où on vient, via `init(data)` sur chaque scène) — y compris désormais après un combat dans le Champ ou le Donjon (`CombatScene` faisait déjà ça pour le Donjon depuis l'incrément 6 ; étendu au Champ ici via `ReturnSceneKey`/`returnSceneStartData`, en évitant de reporter la position quand la destination diffère de la scène de départ — ex. une défaite renvoie désormais à Basse-Combe, pas Valombre, et n'a pas de position à restaurer). `ReturnSceneKey` (`src/ui/returnContext.ts`) inclut maintenant `'Hamlet'`.

**Route allongée + panneaux (suite immédiate)** : plutôt qu'un unique Champ collé à Valombre, la route Basse-Combe → Valombre traverse maintenant 4 zones : Champ → **Forêt** (`ForestScene`, nouvelle) → **Grotte** (`CaveScene`, nouvelle) → Valombre. Le Champ garde ses 3 sorties, mais celle de l'est mène désormais à la Forêt (pas directement à Valombre). La Forêt (400×480, arbres denses décoratifs) a des rencontres aléatoires piochées entre `corrupted_wolf` et un nouveau `goblin_scout`. La Grotte (200×400, couloir façon donjon) fait volontairement l'impasse sur un vrai 2e donjon (pas de grille/boss) mais n'est pas un simple couloir non plus : 2 rencontres fixes contre un nouveau `cave_spider`, avec le même suivi "cleared" que le Donjon (`clearedEncounterIds` + `resume: true` — sans quoi fuir un combat replace le joueur pile sur la même zone de rencontre, qui se re-déclenche aussitôt). `src/ui/signpost.ts` (`addSignpost`) ajoute des panneaux directionnels aux carrefours (près du pont dans le Champ, aux entrées de la Forêt) — juste un poteau + une liste de directions toujours visible, aucune interaction requise.

**Deux bugs réels trouvés en testant cette route** (donc probablement latents ailleurs, corrigés partout) :
1. Chaque scène fait `const save = await SaveManager.load()` en fin de `create()` avant de construire le `CharacterSheetPanel` — mais Arcade Physics continue de tourner à chaque frame pendant ce await, donc si le joueur atteint une zone de sortie *avant* que ce chargement finisse, la scène est arrêtée puis le `create()` encore en attente se réveille et tente de construire un panneau sur une scène déjà détruite, corrompant l'état interne des colliders de Phaser au prochain pas de physique (`Cannot read properties of undefined (reading 'isParent')`, planté dans `collideObjects`). Fix : `if (!this.scene.isActive()) return;` juste après le `await`, dans les 6 scènes concernées (`Hamlet`/`Village`/`Field`/`Forest`/`Cave`/`Dungeon`).
2. Dans `CaveScene`, le point d'arrivée par défaut (`WORLD_HEIGHT - 20`) tombait exactement sur la bordure de la zone de sortie sud — arriver dans la Grotte re-déclenchait instantanément la sortie vers la Forêt. Plusieurs points d'entrée entre zones avaient une marge de 10px à peine (déjà repérable via la leçon de Basse-Combe sur les passages trop étroits) ; élargis à 20-40px partout où c'était le cas.

**Diversification des routes : embranchements + camps (suite immédiate)** : la route restait jusque-là purement linéaire (Basse-Combe → Champ → Forêt → Grotte → Valombre) sans aucun détour ni quête accessible en dehors des hubs. Deux détours optionnels ajoutés, un par grand biome, chacun un cul-de-sac assumé (pas de sur-embranchement) :
- **Camp de bandits** (`BanditCampScene`, nouvelle, clé `BanditCamp`) — embranchement à l'ouest du Champ (au sud de la rivière, donc accessible sans traverser le pont). Un garde blessé donne la quête `bandit_camp_threat` (vaincre 3 `bandit_thug`, nouveau monstre) sur place, 2 rencontres fixes dans le camp avec le même suivi "cleared"/`resume` que la Grotte/le Donjon (une visite normale — via la sortie, pas une fuite en combat — remet les rencontres à zéro, donc le camp reste "farmable" pour la 3e victoire si besoin).
- **Camp de gobelins** (`GoblinCampScene`, nouvelle, clé `GoblinCamp`) — même gabarit, embranchement au nord de la Forêt. Une éclaireuse donne `goblin_camp_threat` (vaincre 3 `goblin_brute`, nouveau monstre), 2 rencontres fixes.

Panneau du Champ mis à jour (`['↓ Basse-Combe', '↑ Repaire du Loup', '→ Forêt (vers Valombre)', '← Camp de bandits']`), panneau de la Forêt idem (`['← Champ', '→ Grotte', '↑ Camp de gobelins']`).

**Rappel systématiquement réappliqué avant même de tester** : le NPC donneur de quête de chaque camp est délibérément placé hors de l'axe spawn→sortie (`(190,185)`, pas sur la ligne centrale) pour éviter la collision-bloque-le-passage déjà rencontrée avec le mentor de Basse-Combe.

**Non-bug trouvé en testant** : un test automatisé donnait `progress: 0` après avoir "marché" sur les deux zones de rencontre du camp de bandits, ce qui ressemblait à un déclenchement raté. En loguant la position du joueur après chaque poussée de joystick, les deux rencontres se déclenchaient bel et bien (positions relevées à l'intérieur des zones 26×26) — le script de test fuyait systématiquement les combats (fuir ≠ vaincre, donc la quête n'avance logiquement pas) et une poussée finale trop longue faisait sortir le joueur du camp par la zone de sortie sud, d'où une capture d'écran finale qui semblait montrer le Champ. Un deuxième test faisant réellement gagner un combat confirme que `progress` avance bien à chaque victoire, pour les deux camps.

**Ferme isolée + petit sanctuaire (suite immédiate)** : `VISION.md` a été étoffé (structure du monde par régions, types de villages/routes, biomes, répartition des donjons — voir sa section "Régions"). La région de départ y est décrite comme "village natal, bourg marchand, ferme isolée, petit sanctuaire" ; les deux premiers existaient déjà (Basse-Combe, Valombre), les deux derniers manquaient. Ajoutés comme deux branches supplémentaires depuis Basse-Combe (ouest et est, les deux seuls côtés encore libres de son pourtour) :
- **La ferme isolée** (`FarmScene`, clé `Farm`) — une fermière donne la quête `crop_pests` (vaincre 3 `field_rat`, nouveau monstre, le plus faible du jeu), rencontres aléatoires en marchant dans les rangs de culture (même modèle que la Forêt : basé sur la distance parcourue, pas de zones fixes, donc pas besoin de suivi cleared/resume ici).
- **Le petit sanctuaire** (`ShrineScene`, clé `Shrine`) — délibérément sans combat (un sanctuaire doit rester une zone sûre) : un ermite offre un soin complet gratuit (PV et PM) et fait défiler quelques lignes de lore sur le Sceau/la corruption à chaque visite. Pas encore de quête associée (candidat naturel pour du contenu "Daedra-style" plus tard).

`ReturnSceneKey` inclut désormais `'Farm'` et `'Shrine'` (ni l'un ni l'autre resumable, pour les raisons ci-dessus).

**Aiglemont, la première cité-État (suite immédiate)** : premier pas hors de la région de départ, vers la région 2 de `VISION.md`. Valombre gagne une deuxième sortie (sud, symétrique de celle vers la Grotte au nord) : **la route commerciale** (`RoadScene`, clé `Road`) — un garde de caravane pour la mise en contexte, rencontres aléatoires en réutilisant `corrupted_wolf` (la route elle-même n'a pas besoin d'identité de menace propre). Elle mène à **Aiglemont** (`CityScene`, clé `City`), la première cité-État : plus grande que Valombre, sol pavé (nouvelle texture générée `stoneTile`), trois bâtiments (Caserne, Tour des Mages, Marché). Volontairement pas de forge — l'artisanat reste l'identité de Valombre, Aiglemont apporte plutôt la première touche politique/faction annoncée pour l'acte 2 dans `DESIGN.md` (sans encore de vrai système de réputation) :
- **Capitaine Bregan** (caserne) donne `city_road_patrol` (vaincre 3 `corrupted_boar`, nouveau monstre, le plus costaud hors donjon actuellement — cohérent avec la règle de loot liée à la progression : plus loin dans l'histoire, plus fort).
- **Mage Sélène** (tour) fait défiler quelques lignes de lore sur la Guilde des Mages et la corruption (même mécanisme que l'ermite du sanctuaire), sans service ni boutique pour l'instant.
- **Marchand** (marché) réutilise `MerchantScene` telle quelle plutôt que dupliquer un système — `MerchantScene` a été généralisée pour accepter un `returnScene: ReturnSceneKey` (défaut `'Village'`, donc les appels existants depuis Valombre restent inchangés) au lieu de renvoyer systématiquement vers Valombre en dur.

**Non-bug trouvé en testant (mais un vrai accroc mineur au passage)** : un test automatisé restait bloqué à une position fixe malgré des poussées de joystick répétées après avoir quitté la boutique du marchand. Cause réelle, pas un artefact de test cette fois : le point de retour depuis `MerchantScene` est la position exacte où le joueur a appuyé sur Action, qui peut atterrir juste contre le petit collider (14×20) du PNJ marchand — bien à l'intérieur de son rayon d'interaction (60px). Une poussée qui longe pile ce bord reste "accrochée" au lieu d'avancer. Ce n'est pas une régression introduite ici : `VillageScene` a exactement le même risque depuis l'incrément 8 (même PNJ marchand, même pattern de retour). En jeu réel, un joueur dévie naturellement le joystick de quelques degrés et se dégage sans même le remarquer — pas de correctif de code pour l'instant, juste noté ici pour référence si ça revient ailleurs.
**Contrôles : passage au tout-tactile (suite immédiate)** : le joystick tactile + bouton "Action" (présents depuis l'incrément 1) sont remplacés partout par un contrôle 100% tactile — plus léger, plus immersif. `src/input/VirtualJoystick.ts` et `src/ui/TouchButton.ts` sont supprimés, remplacés par `src/input/TapController.ts` :
- **Taper une zone vide** → le joueur s'y déplace automatiquement (plus besoin de maintenir un stick).
- **Taper un PNJ, un nœud de récolte, un marchand...** → déclenche l'action directement (peu importe la distance actuelle du joueur), tout en faisant marcher l'avatar vers lui pour la continuité visuelle. Remplace le pattern `handleAction()` + `distanceTo() < INTERACT_RADIUS` répété dans chaque scène par une liste d'`Interactable` (position, rayon de détection, callback) déclarée une fois par scène.
- **Taper un bouton d'interface** (Menu, dialogue, boutons de combat...) → reste le seul ressort de ce bouton, jamais interprété comme un déplacement, grâce à `scene.input.hitTestPointer(pointer)` (si le point tapé touche un objet interactif existant, le tap-déplacement est ignoré).
- Pas de vrai pathfinding : une cible cachée derrière un obstacle fait juste buter le joueur dessus. `TapController` détecte ce blocage (déplacement quasi nul pendant 500ms) et abandonne silencieusement la cible plutôt que de laisser le joueur pousser indéfiniment contre le mur — un vrai joueur ajuste alors son prochain tap, comme il le ferait pour contourner un obstacle avec un stick.
- Le clavier (flèches directionnelles) reste supporté en solution de repli desktop, et prend systématiquement le pas sur une cible tactile en cours.

`src/entities/player.ts` expose maintenant `updatePlayerMovement(player, cursors, moveTarget)` (accepte un point `{x,y} | null` au lieu d'une référence au joystick, retourne si le joueur est toujours en déplacement) au lieu de lire un vecteur de joystick. Les 12 scènes de déplacement (Basse-Combe, Valombre, Champ, Forêt, Grotte, Camp de bandits, Camp de gobelins, Ferme, Sanctuaire, Route commerciale, Aiglemont, Donjon) ont toutes été migrées vers ce nouveau contrôleur. `MerchantScene` a été généralisée avec un `returnScene` paramétrable (déjà fait pour Aiglemont) — sans lien direct avec ce changement mais dans le même lot de commits.

**Accroc réel trouvé en testant (pas un artefact)** : un test tapait un point de sortie calculé au pixel près, et le joueur restait bloqué en boucle contre le coin d'un bâtiment ou du PNJ mentor — plusieurs fois de suite avec des cibles différentes. Confirmé qu'il s'agit bien de la limite "pas de pathfinding" annoncée et acceptée avant ce chantier (une diagonale calculée exactement peut raser un obstacle), pas d'un bug du contrôleur : viser un point clairement dégagé (ex. la ligne centrale x=120 de Basse-Combe, déjà maintenue libre par conception) résout systématiquement le blocage. Un autre "faux positif" similaire : un tap calculé près du coin supérieur gauche de l'écran ne déplaçait jamais le joueur — normal, il tombait pile sur la zone du bouton "Menu" (fixe à l'écran), donc correctement absorbé comme un tap d'interface plutôt que comme un déplacement.
**Densité et vie ambiante (suite immédiate)** : passe dédiée à faire en sorte qu'aucun lieu entre Basse-Combe et Aiglemont ne se lise comme vide, sans toucher au contenu narratif (aucune nouvelle quête dans ce lot). `src/entities/wanderer.ts` (nouveau) : petit PNJ qui fait des allers-retours automatiques entre deux bornes — villageois, citadins, voyageur, animaux de pâture. Compatible avec `TapController` via des getters `x`/`y` sur l'objet `Interactable` (au lieu de valeurs figées), pour que le point taper-pour-interagir suive bien le PNJ en mouvement plutôt que sa position de spawn.
- **Basse-Combe** : 1 villageois ambiant (lignes tournantes, pas de quête).
- **Valombre** : 2 villageois, un puits, deux étals de marché près de la marchande.
- **Aiglemont** : 2 citadins, un étal près du Marché.
- **Route commerciale** : 1 voyageur ambiant en plus du garde de caravane.
- **Le Champ** : 2 moutons qui paissent (interaction légère, pas de dialogue).
- **La Forêt** : densité d'arbres augmentée (+6), champignons décoratifs, 1 cerf craintif.
- **La Grotte** : rochers augmentés, cristaux lumineux décoratifs.

Camp de bandits/gobelins, Ferme et Sanctuaire laissés tels quels dans ce lot (déjà denses pour leur taille, cul-de-sacs courts) — prochain candidat si besoin d'aller plus loin.
**Correctif : cartes plus petites que l'écran (suite immédiate)** : bug signalé par l'utilisateur sur Basse-Combe ("le village du début n'est pas en plein écran") — confirmé bien plus large qu'un seul lieu. La caméra ne peut jamais défiler au-delà des limites du monde (`camera.setBounds`) ; si une carte est plus petite que la résolution interne du jeu (216×384), la caméra reste bloquée et une bande unie de la couleur de fond par défaut du canvas (noir, `#0b0c10`) reste visible en permanence — 9 des 12 scènes de déplacement étaient concernées (seules Valombre, le Champ et Aiglemont avaient une carte assez grande). Corrigé en agrandissant chacune d'elles à au moins 220×400 : Basse-Combe, la Forêt, la Grotte, le Camp de bandits, le Camp de gobelins, la Ferme, le Sanctuaire, la Route commerciale, le Donjon. Zones de sortie/PNJ existants inchangés en position (ils utilisent déjà des formules relatives à `WORLD_WIDTH`/`WORLD_HEIGHT`, donc ils se recalent automatiquement sur les nouveaux bords) ; quelques éléments décoratifs ajoutés dans l'espace nouvellement visible pour éviter un vide (troisième cabane à Basse-Combe, rangées de cultures supplémentaires à la Ferme, pierres dressées au Sanctuaire, etc.).
**Rareté épique + récompenses signature, deux nouveaux donjons (suite immédiate)** : demande explicite du joueur — beaucoup plus de donjons, de difficulté variable, certains "inutiles" (pas de quête) mais avec du butin garanti en fin de parcours, d'autres difficiles avec une récompense particulière en plus du tirage normal. `src/game/item.ts` : `Rarity` gagne un palier `'epic'` (entre rare et légendaire dans l'échelle de `VISION.md`, pas encore entièrement implémentée) ; un `ItemTemplate` peut être marqué `signature: true` pour ne jamais apparaître dans le tirage aléatoire (`rollLootItem`/`LOOTABLE_TEMPLATES`) tout en restant créable directement — un donjon difficile peut ainsi accorder à son boss un objet garanti et exclusif, en plus (pas à la place) du tirage normal (`CombatScene`, `SIGNATURE_REWARDS`).
- **Catacombes d'Aiglemont** (`CatacombsScene`, clé `Catacombs`) — donjon difficile, accessible depuis la sortie sud d'Aiglemont. Même gabarit que le Repaire du Loup mais plus long : 3 rencontres régulières (nouveau monstre `corrupted_knight`) derrière une barrière qui ne s'ouvre qu'une fois les trois déclenchées, boss (`fallen_guardian`, nouveau, 100 PV) donnant systématiquement un tirage de butin (avec une chance d'objet épique) et sa récompense signature garantie.
- **Le vieux puits** (`OldWellScene`, clé `OldWell`) — détour optionnel et volontairement mineur, accessible depuis la sortie sud de la Forêt. Aucune barrière, aucune quête associée : une rencontre régulière (rat des cavernes) puis une rencontre finale qui garantit toujours un peu de butin (`well_guardian`, nouveau monstre faible), sans les meilleures chances de rareté d'un vrai boss.

**Bug réel trouvé en testant (corrigé dans les deux donjons à boss existants)** : la zone de déclenchement du combat de boss n'était jamais retirée après la victoire (contrairement aux rencontres régulières, qui se détruisent une fois déclenchées) — et le point de retour après un combat est exactement la position où le joueur se tenait en le déclenchant. Revenir dans la scène juste après avoir vaincu le boss retombait donc instantanément dans la même zone et relançait aussitôt un nouveau combat, en boucle. Corrigé dans `DungeonScene` (bug préexistant, jamais remarqué jusqu'ici) et `CatacombsScene` : la zone de boss suit maintenant le même principe que les rencontres régulières (détruite au premier déclenchement, suivie dans `clearedMonsterIds`, ne réapparaît pas après un `resume`).

**Quête principale — pont Acte 1 → Acte 2 (suite immédiate)** : jusqu'ici, aucune quête ne reliait le village de départ à Aiglemont — seulement des quêtes locales indépendantes. `src/game/mainQuest.ts` (nouveau) introduit ce fil narrateur, volontairement à part du moteur générique `quest.ts` (conçu pour de simples quêtes "vaincre N monstres" — un fil unique et linéaire n'a pas besoin d'un moteur multi-objectifs générique). `character.mainQuestStage` (`'not_started' | 'dungeon' | 'revelation' | 'aiglemont' | 'complete'`) suit la progression, avec récompense (XP + objet) à chaque étape franchie.
- **Basse-Combe (Aldric)** — une fois la quête locale des loups rendue, une nouvelle conversation avec Aldric démarre le fil principal et pointe vers le Repaire du Loup.
- **Repaire du Loup** — vaincre le boss fait avancer l'étape, quelle que soit la scène de départ du combat (`CombatScene.victory()`, même mécanisme que `advanceQuestsOnDefeat`).
- **Basse-Combe (Aldric)** — retour obligé pour la suite : dialogue de révélation, récompense, et renvoi vers Aiglemont.
- **Aiglemont (Mage Sélène)** — dialogue de conclusion de l'Acte 1, récompense finale ; les lignes de lore habituelles de Sélène reprennent normalement après.

La quête principale apparaît désormais en tête de l'écran Quêtes (`QuestLogScene`), avec son propre statut par étape — séparée de la liste générique des quêtes secondaires.

**Correctif : écran Quêtes défilant (suite immédiate)** : la limite notée ci-dessus (liste débordant sous le bouton "Retour" dès 6 entrées) est corrigée. Les entrées de quêtes vivent maintenant dans un `Phaser.GameObjects.Container` dédié (`QuestLogScene.listContainer`), masqué à la zone visible entre le titre et le bouton "Retour" (`createGeometryMask`), avec défilement au glisser tactile (suivi manuel du delta du pointeur, `container.y` borné). Le bouton "Retour" reste un objet de scène indépendant, hors du container — conforme à la règle déjà notée plus haut (les objets interactifs ne doivent jamais être enfants d'un `Container`) puisque seul le contenu texte, non interactif, est déplacé dedans.

**Faubourg des quais — premier contenu annexe de l'Acte 2 (suite immédiate)** : Aiglemont gagne une sortie est (`FaubourgScene`, clé `Faubourg`), même gabarit que le Camp de bandits/Camp de gobelins (cul-de-sac, 2 rencontres fixes farmables, pas de porte). Un nouveau PNJ ("Renn") donne `faubourg_smugglers` (vaincre 3 `smuggler_thug`, nouveau monstre), quête libre (non bloquée par la quête principale, conformément à la règle de `DESIGN.md` sur les quêtes annexes de l'Acte 2), avec un clin d'œil discret au fil principal (caisses suspectes) sans le nommer explicitement.

**Entrepôt abandonné — troisième palier de difficulté (suite immédiate)** : `WarehouseScene` (clé `Warehouse`), accessible depuis une nouvelle sortie nord du Faubourg des quais. Même mécanique de porte que Catacombes/Repaire du Loup (2 rencontres régulières avant que la porte ne s'ouvre), mais sans récompense signature — juste un boss (`smuggler_captain`, nouveau) au butin garanti, positionné entre Le vieux puits (facile) et les Catacombes d'Aiglemont (difficile) sur l'échelle de difficulté demandée par le joueur. Au passage, corrige la même zone de boss jamais retirée après victoire que Catacombes/Repaire du Loup (voir plus haut) — implémentée ici dès le départ avec le correctif.

**Ressources avec rareté + recettes améliorées (suite immédiate)** : jusqu'ici les ressources (`src/game/material.ts`) n'avaient qu'un seul palier (fer brut, herbe), sans lien avec la rareté des objets. Chaque ressource gagne désormais une variante rare, stockée comme un id à part (`iron_ore_rare`, `herb_rare`) plutôt qu'une forme imbriquée — même compteur plat `character.materials`, pas de migration de sauvegarde nécessaire. Nouvelle ressource `leather`/`leather_rare` (cuir / cuir supérieur), qui ne se récolte pas mais se droppe au combat.
- **Récolte** (`FieldScene`) : chaque récolte donne toujours son unité commune, avec une petite chance additionnelle de trouver aussi la variante rare.
- **Combat** (`CombatScene`) : les monstres de type "bête" (loups, loup alpha, sanglier corrompu) ont une chance de faire tomber du cuir en plus du butin normal — chance modeste sur un combat classique, chance plus élevée (et variante supérieure) si c'est un boss. Les autres monstres n'en donnent pas.
- **Artisanat** (`src/game/recipe.ts`) : 3 nouvelles recettes consommant la ressource rare *en plus de* la commune (pas à sa place, pour que les deux paliers restent utiles) : épée courte de qualité supérieure, gants de cuir de qualité supérieure (tous deux en rareté rare plutôt que commune), et une potion de soin supérieure (nouveau consommable, soigne 50 PV contre 25). `CraftingScene` gagne une pagination (2 recettes par page) pour ne pas déborder de l'écran avec 5 recettes désormais listées — au passage, corrige un bug préexistant où les boutons "Fabriquer" s'accumulaient sans jamais être vidés d'une visite à l'autre (invisible pour un joueur réel, qui ne clique jamais que ce qui est affiché, mais un vrai accroc de gestion d'état).
- Le Sac colore désormais les ressources rares en bleu (même palette que la rareté des objets) dans l'onglet Ressources.

**Les Archives scellées — cinquième donjon (suite immédiate)** : `ArchivesScene` (clé `Archives`), accessible depuis une nouvelle sortie nord d'Aiglemont (derrière la Tour des Mages). Même gabarit que Le vieux puits : pas de porte, pas de quête, un combat régulier puis une rencontre finale au butin garanti. Complète l'échelle de difficulté avec un deuxième détour "facile" (le premier, Le vieux puits, est en région 1) — 5 donjons au total désormais, de facile à difficile.

**Le Faubourg des quais devient une chaîne à deux étapes (suite immédiate)** : plutôt que de forcer une nouvelle direction cardinale à Aiglemont (les quatre côtés sont désormais occupés : route commerciale à l'ouest, Catacombes au sud, Faubourg à l'est, Archives au nord), ce lot approfondit ce qui existe déjà. Une fois `faubourg_smugglers` rendue, Renn (l'informateur) propose une suite (`faubourg_smugglers_leader`) pointant vers le boss de l'Entrepôt abandonné (`smuggler_captain`, déjà en jeu) — le moteur de quêtes générique suffisait tel quel (objectif "vaincre 1 fois"), aucune extension nécessaire. Ça donne enfin une vraie raison narrative à l'Entrepôt au-delà d'être un donjon isolé, chaîne courte cohérente avec la note de `DESIGN.md` sur les quêtes de faction de l'Acte 2.

**Route fluviale + Relais des chasseurs — premier pas vers la région 3 (suite immédiate)** : les quatre côtés d'Aiglemont étant occupés, cette extension part du Faubourg des quais plutôt que de la ville elle-même. `RiverRoadScene` (clé `RiverRoad`) est une route linéaire façon Route commerciale/Forêt (rencontres aléatoires à la marche), avec son propre monstre de biome (`marsh_serpent`, marais) plutôt qu'un monstre réutilisé — première identité de menace propre à la région 3. Elle mène au `HunterOutpostScene` (clé `HunterOutpost`), un petit hub sûr (pas de rencontre aléatoire, comme Valombre/Aiglemont) où une chasseuse donne `marsh_patrol` (vaincre 3 `marsh_serpent`, rencontrés sur la route plutôt qu'au relais). Volontairement un cul-de-sac pour l'instant — pas encore de vraie ville de région 3, juste le premier jalon géographique.

**La quête principale continue au-delà d'Aiglemont (suite immédiate)** : plutôt que d'ajouter encore une nouvelle zone, cette extension approfondit le fil principal, qui s'arrêtait jusqu'ici juste après le pont Acte 1 → Acte 2. Trois nouvelles étapes dans `src/game/mainQuest.ts` (`catacombs`, `trail_found`, `debriefed`) : Sélène propose une piste concrète pointant vers un donjon déjà en jeu (les Catacombes d'Aiglemont, jusqu'ici sans quête associée), vaincre son boss fait avancer l'étape (même mécanisme générique que pour le Repaire du Loup — `BOSS_TRANSITIONS` dans `mainQuest.ts` généralise ce qui était propre à `alpha_wolf`), puis un débriefing final verse la récompense et clôt ce chapitre (en laissant la porte ouverte pour la suite). Mêmes bénéfices que pour le Faubourg/Entrepôt : donne une vraie raison narrative à un donjon existant plutôt que d'en ajouter un nouveau.

**La Tanière des marais — sixième donjon (suite immédiate)** : `MarshLairScene` (clé `MarshLair`), accessible depuis une nouvelle sortie nord du Relais des chasseurs. Même gabarit "moyen" que l'Entrepôt abandonné (porte à deux rencontres, boss au butin garanti, pas de récompense signature), avec le monstre de biome de la région 3 (`marsh_serpent`) plus une nouvelle matriarche (`marsh_matriarch`, boss). La quête du relais (`marsh_patrol`) devient elle aussi une chaîne à deux étapes — même schéma que le Faubourg/Entrepôt : une fois rendue, la chasseuse pointe vers ce donjon plutôt que de le laisser sans quête associée.

**Carte accessible depuis le Menu (suite immédiate)** : nouveau bouton "Carte" (`MapScene`, clé `Map`) dans le Menu, désormais une grille 4×2 (Quitter seul sur la dernière ligne). Schématique, pas à l'échelle — vu le nombre de lieux (20), une vraie carte géographique aurait demandé un canvas déplaçable/zoomable ; à la place, deux onglets ("Région 1" / "Aiglemont", même mécanisme d'onglets que le Sac) affichent chacun leurs lieux sur une grille fixe reliée par des lignes selon les connexions réelles entre scènes (`src/game/worldMap.ts`). Le point où se trouve le joueur est mis en évidence, ainsi que tout lieu concerné par une quête non terminée (quête principale à l'étape en cours + quêtes secondaires acceptées ou complétées mais pas encore rendues) — l'onglet qui s'ouvre par défaut est celui de la région où le joueur se trouve actuellement.

**Coffres à butin aléatoire (suite immédiate)** : `src/game/chest.ts` (`isChestOpened`/`openChest`/`chestLootMessage`), un coffre par lieu isolé/abandonné — Basse-Combe, Repaire du Loup, Catacombes d'Aiglemont, Entrepôt abandonné, Tanière des marais, Grotte (6 au total). Butin garanti à l'ouverture (contrairement au combat, jamais de coffre vide), avec de meilleures chances de rare/épique qu'un monstre classique (35% / 8%, via `rollLootItem({ guaranteed: true, rareChance: 0.35, epicChance: 0.08 })`). Chaque coffre ne se remplit qu'une fois : l'état ouvert est persisté par lieu (`character.openedChests`), le coffre s'affiche visuellement grisé une fois vidé et le reste après rechargement de la sauvegarde ; retaper dessus affiche simplement "Ce coffre est vide." sans nouveau tirage.

**Trame principale prolongée au-delà de `debriefed` (suite immédiate)** : `MainQuestStage` gagne 3 nouvelles étapes (`faubourg_lead` → `shard_confirmed` → `shards_beyond`, ce dernier remplaçant `debriefed` comme état terminal). Reprend et prolonge Sélène (`CityScene.talkToMage()`) plutôt que d'ouvrir une nouvelle ville, en s'appuyant sur le contenu déjà en place au Faubourg/Entrepôt abandonné (`smuggler_captain`, déjà utilisé par la quête secondaire `faubourg_smugglers_leader`) via une nouvelle entrée dans `BOSS_TRANSITIONS` — la mort de ce boss fait donc à la fois progresser la quête secondaire existante et la trame principale, sans conflit (même mécanisme que `fallen_guardian`/Catacombes). Récompense à l'étape finale : 180 XP + Amulette simple (Épique). `QuestLogScene.MAIN_QUEST_STATUS` et `worldMap.ts`'s `MAIN_QUEST_LOCATION` mis à jour pour les 3 nouvelles étapes (dont un nouveau marqueur de quête sur l'Entrepôt, le temps de cette étape).

**Nouvelle quête secondaire au petit sanctuaire (suite immédiate)** : l'ermite (`ShrineScene`) donnait déjà un soin gratuit complet et un peu de lore, sans quête. Ajout de `shrine_pilgrims` (`src/game/quest.ts`, moteur générique existant) — des gobelins éclaireurs qui harcèlent les pèlerins en Forêt, réutilisant un monstre déjà présent (`goblin_scout`, rencontres aléatoires de la Forêt) sans en ajouter un nouveau. Le soin gratuit reste accessible à tout moment (bouton présent quel que soit l'état de la quête), pour ne pas transformer le sanctuaire en donneur de quête au prix de son rôle de havre de repos.

**Chaîne de quête à 2 étapes au Camp de bandits (suite immédiate)** : `bandit_camp_threat` (existante) gagne une suite, `bandit_camp_threat_leader`, sur le même principe que Faubourg/Entrepôt — sauf que ce camp reste un cul-de-sac par conception (pas de nouvelle scène connectée) : le chef des bandits (`bandit_leader`, nouveau monstre, 40 PV/8 ATQ, boss) est simplement placé plus profondément dans le camp existant. Comme `smuggler_captain`, il est toujours affrontable indépendamment de la quête ; celle-ci ne fait que suivre/récompenser la même victoire. Récompense : 100 XP + Casque de cuir (Rare).

**Même traitement pour le Camp de gobelins (suite immédiate)** : `goblin_camp_threat` gagne `goblin_camp_threat_leader`, copie conforme du schéma bandits/chef — un chef des gobelins (`goblin_chief`, nouveau monstre, 44 PV/9 ATQ, boss) placé plus profondément dans le camp, toujours affrontable indépendamment de la quête. Récompense : 105 XP + Gants de cuir (Rare). Les deux camps annexes du Champ/Forêt ont maintenant une profondeur de quête symétrique.

**Et pour la Ferme isolée (suite immédiate)** : `crop_pests` gagne `crop_pests_king` — même principe, adapté à une scène qui n'avait jusqu'ici que des rencontres aléatoires (pas de zone fixe) : ajout d'un « Roi des rats » (`rat_king`, nouveau monstre, 26 PV/5 ATQ, boss) sur une zone fixe toujours présente dans la ferme, avec le suivi cleared/resume nécessaire (`Farm` ajoutée à `RESUMABLE_SCENES`). Récompense : 60 XP + Épée courte (Rare) — première quête secondaire à offrir une arme plutôt qu'un équipement défensif.

**Correction : `city_road_patrol` était impossible à terminer** — la quête (donnée par le capitaine d'Aiglemont) demande de vaincre 3 `corrupted_boar`, mais `RoadScene` (la route commerciale où elle se déroule) tirait ses rencontres aléatoires parmi `corrupted_wolf`, jamais `corrupted_boar` : ce monstre n'était généré nulle part dans le jeu. Un audit rapide (chaque identifiant de `monster.ts` doit apparaître dans au moins une scène) a confirmé que c'était le seul cas. Corrigé en faisant tirer `RoadScene` sur `corrupted_boar` à la place — cohérent avec le texte de la quête, qui parlait déjà de sangliers sur cette route.

**Chaîne à 2 étapes pour `city_road_patrol` (suite immédiate)** : `city_road_patrol_alpha` — un sanglier alpha corrompu (nouveau monstre, boss, 48 PV/10 ATQ) sur une zone fixe toujours présente sur la Route commerciale (`Road` ajoutée à `RESUMABLE_SCENES`). Split identique à Faubourg/Entrepôt : le dialogue des deux étapes reste chez le capitaine (`CityScene`), le combat lui-même se déroule ailleurs (`RoadScene`). Récompense : 110 XP + Anneau simple (Rare).

**Coffres dans les 3 dernières zones d'exploration sans quête (suite immédiate)** : Le vieux puits, Route fluviale et Archives scellées n'avaient jamais reçu de coffre lors du premier lot (increment 9). Complété avec le même système (`isChestOpened`/`openChest`) — ces trois scènes n'avaient pas toutes le mécanisme d'interaction tactile en place (Le vieux puits et Archives scellées n'avaient encore aucun élément tapable), donc premier ajout de `Interactable`/`tapControl.setInteractables` là où c'était absent. Le coffre des Archives et celui du Vieux puits ne nécessitent pas de suivi "cleared" par scène (contrairement aux boss) : l'état ouvert vit entièrement dans `character.openedChests`, persistant et suffisant. Les 6 donjons + ces 3 zones portent maintenant le total à 9 coffres.

**Trame principale prolongée une nouvelle fois, au-delà de `shards_beyond` (suite immédiate)** : 2 nouvelles étapes (`trail_west` → `river_lead`, ce dernier remplaçant `shards_beyond` comme état terminal). Sélène (`CityScene`) envoie le joueur au Relais des chasseurs plutôt que d'ouvrir une nouvelle carte — reprend un lieu déjà bâti (région 3) et son PNJ existant (`HunterOutpostScene`, une fois la propre chaîne de quêtes du relais terminée) plutôt que d'introduire un nouveau donneur de quête. Récompense à l'étape finale : 220 XP + Plastron de cuir (Épique).

**Premiers effets sonores (suite immédiate, entame l'incrément 10)** : `src/ui/sound.ts` — sons procéduraux via l'API Web Audio brute (oscillateurs + enveloppe de gain), aucun fichier audio requis, donc la PWA reste 100% autonome hors-ligne. Le premier tap du joueur (bouton du Titre) débloque l'`AudioContext` pour le reste de la session, conformément aux politiques de lecture automatique des navigateurs. Branché sur : coup porté/reçu et victoire/montée de niveau/défaite en combat, ouverture des 9 coffres, achat/vente chez la marchande, fabrication réussie à la forge/l'alchimie, et — en suivi immédiat — tous les rendus de récompense de quête (les 8 quêtes secondaires et les 5 étapes de la trame principale qui remettent une récompense). Premier contenu de l'incrément 10 (jusque-là à 0% : uniquement des rectangles de couleur, aucun son).

**Fin d'Acte 1 de la trame principale — v1 fonctionnellement complète (suite immédiate)** : nouvelle étape terminale `act1_complete`, atteinte via une dernière conversation avec Sélène qui clôt le fil narratif proprement (reconnaissance de la menace, mise en pause volontaire avant les cités du fleuve/Acte 2) plutôt que de laisser la quête sur une piste sans fin. `QuestLogScene` affiche désormais un statut dédié « Acte 1 terminé » avec un message explicite indiquant que la suite arrive dans une prochaine mise à jour — pensé pour que la fin de contenu se lise comme une pause voulue, pas comme un jeu inachevé. Récompense de clôture : 260 XP + Bouclier en bois (Épique). Avec cette fin propre, toute la portée v1 (voir section dédiée plus haut) est cochée.

**Lancement de l'Acte 2 — les Terres Noyées (entame l'Acte 2)** : l'étape terminale `act1_complete` (pensée comme une pause volontaire, pas une fin définitive) devient un tremplin plutôt qu'un cul-de-sac, via 2 nouvelles étapes (`crossing_marshes` → `vasenoire_arrival`). Sélène (`CityScene`) envoie le joueur vers une toute nouvelle région (`terresnoyees`, troisième onglet de `MapScene` aux côtés de « Région 1 » et « Aiglemont »), accessible via une nouvelle sortie est du Relais des chasseurs. `SunkenRoadScene` (clé `SunkenRoad`) est une route d'exploration au même gabarit que la Route fluviale : rencontres aléatoires (nouveau monstre de biome `bog_wraith`), un PNJ de couleur locale, un coffre (`sunkenroad_chest_1`, même système que les 9 coffres existants). Elle mène à `VasenoireScene` (clé `Vasenoire`), premier hub sûr de l'Acte 2 (aucune rencontre aléatoire, comme Valombre/Aiglemont) : un PNJ (Yenn) y porte la trame principale et introduit une nouvelle faction pour l'instant purement narrative (les Limaneux) — sans modèle de réputation associé, volontairement laissé de côté tant qu'aucun contenu de quête concret n'en dépend, plutôt que d'ajouter une mécanique qui resterait inutilisée. Récompense à l'étape finale : 300 XP + Casque de cuir (Épique). `QuestLogScene.MAIN_QUEST_STATUS` et `worldMap.ts`'s `MAIN_QUEST_LOCATION`/`MAP_LOCATIONS`/`MAP_CONNECTIONS` mis à jour pour la nouvelle région et les 2 nouvelles étapes.

**Vasenoire s'étoffe — marchande, forge et première quête secondaire (suite immédiate)** : `CraftingScene` gagne un paramètre `returnScene` (même mécanisme que `MerchantScene` depuis le début), jusqu'ici câblée en dur sur un retour à Valombre — nécessaire pour qu'une forge ailleurs que Valombre soit possible sans dupliquer la scène. Vasenoire dispose désormais de son propre étal (`Merchant`) et de sa propre forge (`Crafting`), les deux scènes globales déjà utilisées ailleurs dans le jeu. Yenn porte en plus une première quête secondaire locale (`vasenoire_ruins`, moteur générique existant), réutilisant `bog_wraith` (déjà présent sur la Route engloutie) plutôt que d'ajouter un nouveau monstre — même logique que `shrine_pilgrims`/`goblin_scout`. Accessible une fois la trame principale à l'étape `vasenoire_arrival`, sur le même schéma NPC-unique-qui-enchaîne-les-sujets que le chasseur du Relais (trame principale → quête locale une fois le point d'étape atteint).

**Les Ruines englouties — septième donjon (suite immédiate)** : `SunkenRuinsScene` (clé `SunkenRuins`), accessible depuis une entrée dédiée sur la Route engloutie. Même gabarit "moyen" que la Tanière des marais/l'Entrepôt (porte à deux rencontres, boss au butin garanti, pas de récompense signature), réutilisant `bog_wraith` pour les rencontres derrière la porte et un nouveau boss (`ruins_delver`) au cœur du donjon. `vasenoire_ruins` gagne une suite à 2 étapes (`vasenoire_ruins_leader`, même schéma que Faubourg/Entrepôt ou Relais/Tanière) : Yenn enchaîne dessus une fois la première quête rendue. Un coffre supplémentaire au fond du donjon porte le total à 11.

**La trame principale rouvre après vasenoire_arrival (suite immédiate)** : nouvelle étape `delta_conspiracy`, qui remplace `vasenoire_arrival` comme état terminal. Reprend la quête locale de Vasenoire (`vasenoire_ruins_leader`) plutôt que d'ouvrir un nouveau lieu — une fois cette chaîne rendue, reparler à Yenn déclenche la suite du fil principal au lieu de la ligne de fermeture générique, même schéma que Faubourg/Entrepôt en Acte 1 (une quête secondaire locale qui rouvre la trame principale). Récompense à l'étape finale : 130 XP + Épée courte (Rare). `QuestLogScene.MAIN_QUEST_STATUS` mis à jour ; pas d'entrée dans `worldMap.ts`'s `MAIN_QUEST_LOCATION` pour cette étape (chapitre refermé, même traitement que `debriefed`).

**La trame principale rouvre une nouvelle fois — le réseau des contrebandiers (suite immédiate)** : 3 nouvelles étapes (`limaneux_lead` → `network_exposed` → `smugglers_unmasked`, ce dernier remplaçant `delta_conspiracy` comme état terminal). Yenn envoie le joueur vers un nouveau donjon, `ClandestineDockScene` (clé `ClandestineDock`, accessible depuis une nouvelle sortie nord de Vasenoire), même gabarit "moyen" que les précédents (porte à deux rencontres, boss au butin garanti). Réutilise délibérément `smuggler_thug` (Faubourg, Acte 1) pour les rencontres plutôt qu'un nouveau monstre — la révélation de fin de chaîne (`smuggler_lieutenant`, nouveau boss) relie explicitement le réseau de contrebandiers déjà croisé à Aiglemont à ce qui se passe dans les Terres Noyées, plutôt que d'introduire un antagoniste totalement nouveau à ce stade. Même mécanisme boss→dialogue→récompense que le reste de la trame (`BOSS_TRANSITIONS` pour l'avancée sans récompense au moment du kill, récompense à la conclusion du débriefing suivant). Récompense à l'étape finale : 220 XP + Plastron de cuir (Épique). Douzième coffre du jeu.

**Vasenoire gagne une quête secondaire indépendante de la trame principale (suite immédiate)** : jusqu'ici, les deux seules quêtes de Vasenoire (`vasenoire_ruins`, `vasenoire_ruins_leader`) rouvraient toutes les deux le fil principal — aucune n'était une simple quête annexe comme dans les autres lieux (`wolves_threat`, `crop_pests`, etc.). Nouveau PNJ, Toma (un pêcheur), avec `vasenoire_fisherman` (moteur générique existant), réutilisant `smuggler_thug` (déjà présent au Quai clandestin) plutôt qu'un nouveau monstre. Récompense : 90 XP + Gants de cuir (Rare). Vasenoire est maintenant à 3 quêtes, plus proche de la profondeur des autres hubs.

**La trame principale boucle vers Aiglemont — clôture de ce chapitre (suite immédiate)** : nouvelle étape terminale `network_reported`, qui remplace `smugglers_unmasked`. Contrairement aux étapes précédentes qui reprenaient à Vasenoire, celle-ci ramène le joueur vers un lieu et un PNJ déjà connus (Sélène, `CityScene`, Acte 1) plutôt que d'introduire un nouveau point de chute — clôture le fil ouvert par la révélation du réseau de contrebandiers en le reliant explicitement au capitaine du Faubourg (Acte 1), sur le même principe de fermeture propre que `act1_complete` (« reposez-vous, la suite viendra plus tard » plutôt qu'un vrai cul-de-sac). Récompense : 200 XP + Anneau simple (Épique). `worldMap.ts`'s `MAIN_QUEST_LOCATION[smugglers_unmasked]` pointe désormais vers Aiglemont (`City`) plutôt que Vasenoire ; pas d'entrée pour `network_reported` (chapitre refermé, même traitement que `debriefed`/`delta_conspiracy`).

**L'écran Équipement compare enfin toutes les variantes possédées (suite immédiate)** : toucher un emplacement (ex. Bottes) affichait jusqu'ici uniquement l'objet équipé, avec pour seule action « Déséquiper ». Il liste désormais l'objet équipé *et* tous les objets du Sac correspondant à cet emplacement (même logique de catégorie que `resolveEquipSlot` du Sac — `ring1`/`ring2` acceptent tout objet de catégorie `ring`), chacun avec une flèche ▲/▼ de comparaison (réutilise `isUpgrade`, déjà utilisé par le Sac). Toucher une ligne du Sac l'équipe directement dans l'emplacement précisément touché (pas de résolution automatique ring1/ring2 comme le fait le Sac, puisque le joueur a déjà choisi l'emplacement en le touchant) ; toucher la ligne équipée la déséquipe. Un emplacement vide affiche désormais aussi les objets disponibles pour lui, plutôt que de rester inerte au toucher.

**La trame principale rouvre une dernière fois — le sanctuaire scellé, huitième donjon (suite immédiate)** : 3 nouvelles étapes (`sealed_vault_lead` → `vault_uncovered` → `shard_cache_found`, ce dernier remplaçant `network_reported` comme état terminal). Sélène (`CityScene`) envoie le joueur vers un nouveau donjon, `SealedSanctuaryScene` (clé `SealedSanctuary`), accessible depuis une seconde sortie du Quai clandestin plutôt qu'une nouvelle zone d'exploration — même gabarit "moyen" que les précédents (porte à deux rencontres, boss au butin garanti). Introduit un nouveau monstre ambiant, `corrupted_sentinel`, partagé avec la Route engloutie (voir plus bas), et un nouveau boss (`shard_warden`) qui verse en plus une récompense signature dédiée (`shard_pendant`, Amulette épique, jamais dans la table de butin générale — même mécanisme que l'Amulette du Gardien déchu). Même schéma boss→dialogue→récompense que le reste de la trame. Récompense à l'étape finale : 240 XP + Gants de cuir (Épique). Treizième coffre du jeu.

**La Route engloutie gagne sa première vraie quête secondaire (suite immédiate)** : le PNJ réfugié, jusqu'ici purement décoratif (quelques répliques d'ambiance sans quête), devient un donneur de quête nommé (Odren) avec `sunkenroad_sentinels` (moteur générique existant), réutilisant `corrupted_sentinel` (introduit avec le sanctuaire scellé ci-dessus). Les rencontres aléatoires de la zone alternent désormais entre `bog_wraith` et `corrupted_sentinel` au lieu de toujours tirer le premier, pour que cette nouvelle quête reste faisable sans passer par le donjon. Récompense : 100 XP + Casque de cuir (Rare).

**La trame principale rouvre une dernière fois — un nom, pas encore un lieu (suite immédiate)** : 3 nouvelles étapes (`rival_hunters_lead` → `rival_hunters_confirmed` → `threat_acknowledged`, ce dernier remplaçant `shard_cache_found` comme état terminal). Contrairement aux extensions précédentes, celle-ci est purement narrative (aucun nouveau combat, monstre ou donjon) : Sélène (`CityScene`) renvoie le joueur vers Yenn (`VasenoireScene`, nouvelle méthode `talkToYennAboutRivals`) plutôt que vers un nouveau PNJ, qui confirme l'existence d'un groupe rival cherchant les éclats du sceau depuis des années — sans lien avec le réseau de contrebandiers déjà démantelé. Clôt le fil sur une révélation plutôt qu'une victoire, en préparant un antagoniste pour la suite sans encore lui donner de lieu ni de donjon associé. Récompense à l'étape finale : 200 XP + Casque de cuir (Épique).

**Les Chercheurs d'éclats obtiennent un visage — neuvième donjon (suite immédiate)** : 3 nouvelles étapes (`chercheurs_lead` → `seekers_confronted` → `seekers_defeated`, ce dernier remplaçant `threat_acknowledged` comme état terminal). Plutôt qu'une nouvelle zone d'exploration, le passage caché se trouve à l'intérieur même du Sanctuaire scellé (`SealedSanctuaryScene`, nouvelle sortie nord vers `ShardSeekersCampScene`) — cohérent avec la révélation précédente comme quoi la faction rivale connaissait déjà ce lieu. Même gabarit "moyen" que les 8 donjons précédents, avec un nouveau monstre d'escorte (`seeker_scout`) et un nouveau boss (`seeker_archivist`, le plus coriace à ce jour : 95 PV/15 ATQ) qui verse une récompense signature dédiée (`seeker_signet`, premier anneau signature du jeu — jusqu'ici seules des amulettes en avaient un). Même schéma boss→dialogue→récompense que le reste de la trame. Récompense à l'étape finale : 260 XP + Jambières de cuir (Épique).

**La trame principale nomme enfin l'objectif de la fin d'Acte 2 (suite immédiate)** : nouvelle étape terminale `brotherhood_tomb_hinted`, qui remplace `seekers_defeated`. Purement narrative (aucun nouveau combat, monstre ou lieu) : Sélène décortique les carnets saisis sur l'archiviste et découvre que les Chercheurs d'éclats ne travaillaient pas pour leur propre compte — ils cherchaient à localiser le tombeau de la confrérie fondatrice pour le compte de quelqu'un d'autre, encore non identifié. Reprend et prolonge directement le pitch de `VISION.md`/`DESIGN.md` (la fin d'Acte 2 prévue de longue date autour de ce tombeau), sans encore construire le donjon associé — nomme la cible avant de la rendre jouable, même logique que `act1_complete` avant `crossing_marshes`. Récompense à l'étape finale : 280 XP + Épée courte (Épique) — première arme épique du jeu.

**Fin d'Acte 2 de la trame principale — dixième donjon, premier ennemi du Roi Démon (suite immédiate)** : 3 nouvelles étapes (`tomb_location_found` → `tomb_raided` → `act2_complete`, ce dernier remplaçant `brotherhood_tomb_hinted`). Le tombeau de la confrérie fondatrice, nommé à l'étape précédente, se révèle caché sous les Ruines englouties elles-mêmes (`SunkenRuinsScene`, nouvel escalier caché vers `BrotherhoodTombScene`) plutôt que dans une zone totalement nouvelle — cohérent avec "ce que vous preniez pour le fond des ruines n'était peut-être qu'un plafond". Même gabarit "moyen" que les 9 donjons précédents, réutilisant `corrupted_knight` (déjà l'identité "chevalier déchu" du jeu, Catacombes) pour les gardiens plutôt qu'un nouveau monstre d'ambiance. Le boss, `demon_envoy`, est la première créature du jeu directement au service du Roi Démon plutôt que d'une faction humaine, et le combat le plus dur à ce jour (110 PV/17 ATQ) — sans récompense signature dédiée, puisque narrativement le joueur arrive trop tard : l'émissaire est vaincu, mais un éclat majeur a déjà été extrait du tombeau avant la confrontation, conformément à la fin d'Acte 2 prévue de longue date dans `VISION.md`/`DESIGN.md` ("un donjon majeur est pillé par l'antagoniste, qui vole un éclat majeur — une région entière tombe en corruption"). `act2_complete` reçoit le même traitement que `act1_complete` en son temps : statut dédié « Acte 2 terminé » dans `QuestLogScene`, avec message explicite indiquant que la suite (Acte 3) arrivera dans une prochaine mise à jour, plutôt que de laisser la trame sur une piste sans fin. Récompense de clôture : 300 XP + Bottes de cuir (Épique).

**Premiers signes de l'Acte 3 — la corruption remonte vers l'Aiglemont (suite immédiate)** : 2 nouvelles étapes (`outpost_corruption_lead` → `corruption_confirmed`, ce dernier remplaçant `act2_complete` comme état terminal). Purement narrative, aucun nouveau lieu ni combat : Sélène envoie le joueur vérifier des rapports inquiétants au Relais des chasseurs (déjà bâti, à la frontière entre l'Aiglemont et les Terres Noyées) plutôt que d'ouvrir une nouvelle carte — le chasseur confirme sur place les premiers signes tangibles de corruption (pièges désertés, végétation noircie), même schéma que `trail_west`→`river_lead` en Acte 1 (récompense versée sur place par le PNJ local, pas au retour chez Sélène). `corruption_confirmed` reçoit le même traitement « chapitre clos » que `act2_complete` avant lui (statut dédié dans `QuestLogScene`, mention explicite que l'Acte 3 arrivera dans une prochaine mise à jour) — le statut spécial se déplace donc d'une étape terminale à la suivante à mesure que l'histoire progresse, plutôt que de s'accumuler. Récompense à l'étape finale : 150 XP + Anneau simple (Rare).

**Onzième donjon — la corruption prend forme au Relais des chasseurs (suite immédiate)** : 3 nouvelles étapes (`blighted_grove_lead` → `grove_purified` → `corruption_contained`, ce dernier remplaçant `corruption_confirmed` comme état terminal, et récupérant le statut dédié « Acte 2 terminé »). Le chasseur, une fois les premiers signes confirmés, indique une nouvelle sortie sud du Relais menant à `BlightedGroveScene` — même gabarit "moyen" que les 10 donjons précédents. Nouveau monstre d'escorte (`blight_spawn`) et nouveau boss (`corruption_heart`), premier ennemi né directement de l'éclat volé plutôt qu'une faction ou un serviteur du Roi Démon — explicitement présenté comme une simple retombée locale, pas la source véritable, pour ne pas anticiper sur le climax de l'Acte 3 à venir. Butin signature dédié : `purified_breastplate` (Cuirasse purifiée), premier objet signature à occuper l'emplacement torse plutôt qu'amulette ou anneau. Récompense à l'étape finale : 260 XP + Anneau simple (Épique).

**Le site originel du scellement est enfin nommé (suite immédiate)** : nouvelle étape terminale `original_site_revealed`, qui remplace `corruption_contained`. Purement narrative, aucun nouveau lieu ni combat : en recoupant les carnets du tombeau avec le foyer de corruption du bosquet, Sélène identifie le site où la confrérie fondatrice a scellé le Roi Démon trois siècles plus tôt — le petit sanctuaire de Basse-Combe (`ShrineScene`), déjà visité dès le tout début du jeu et dont une ligne de dialogue existante ("Ce lieu est plus ancien que Basse-Combe elle-même...") préfigurait déjà ce rebondissement sans le confirmer. Reprend exactement la logique du pitch de `VISION.md`/`DESIGN.md` pour la fin d'Acte 3 (site final = version corrompue/miroir d'un lieu déjà visité) — nomme le lieu avant de le transformer en donjon, même schéma que `brotherhood_tomb_hinted` avant `tomb_location_found`. Le statut dédié dans `QuestLogScene` passe de « Acte 2 terminé » à « Acte 3 en cours », pour refléter que la trame a maintenant clairement quitté la clôture de l'Acte 2. Récompense à l'étape finale : 280 XP + Casque de cuir (Épique).

**Le site originel du scellement se révèle un douzième donjon — et le premier visage de l'antagoniste (suite immédiate)** : 3 nouvelles étapes (`shrine_lead` → `seal_failing` → `antagonist_glimpsed`, ce dernier remplaçant `original_site_revealed` comme état terminal, et récupérant le statut dédié « Acte 3 en cours »). Le sanctuaire déjà visité (`ShrineScene`) gagne un passage caché sous son autel décoratif plutôt qu'une nouvelle zone de carte, menant à `SealChamberScene` — même gabarit "moyen" que les 11 donjons précédents. Nouveau monstre d'escorte (`brotherhood_specter`) et nouveau boss, `primordial_guardian`, la magie même du rituel de scellement originel devenue hostile — le combat le plus dur du jeu à ce jour (130 PV/19 ATQ). Butin signature dédié : `sealed_blade` (Lame scellée), première arme à occuper l'emplacement signature plutôt qu'amulette, anneau ou torse. Le débriefing qui suit introduit pour la première fois une présence rivale sur la piste du joueur, entraperçue sans être affrontée — reste délibérément non identifiée, cohérent avec le choix déjà noté dans `VISION.md`/`DESIGN.md` de ne pas en faire un simple méchant caricatural. Récompense à l'étape finale : 350 XP + Bouclier en bois (Épique).

**Rencontres élites et légendaires (suite immédiate)** : chaque rencontre non-boss (donjon comme rencontre aléatoire du Champ) a désormais une petite chance de faire face à une version renforcée du même monstre plutôt qu'à sa version normale — même identité, pas un nouveau monstre à part (ex. un simple loup corrompu du Champ peut devenir un « loup corrompu élite » ou « légendaire »). 4% de chance élite (PV x1.6, ATQ x1.3, XP/or x1.5, butin garanti avec les mêmes probabilités rare/épique qu'un boss), 1% de chance légendaire (PV x2.5, ATQ x1.7, XP/or x2.5, butin garanti avec de meilleures probabilités qu'aucun boss actuel). Différenciation visuelle sans nouvel asset : couleur du nom et teinte de la silhouette de combat reprenant les couleurs de rareté des objets déjà connues du joueur (bleu = élite, violet = légendaire), message d'apparition distinct. Les combats de boss restent volontairement inchangés (invariant appliqué directement dans `createMonster`, y compris contre un appel explicite) : ils sont déjà la version la plus forte et la mieux récompensée d'eux-mêmes par construction. Aucun nouveau palier de rareté d'objet introduit — le système reste dans l'échelle commun/rare/épique déjà en place, légendaire/unique restant réservés à une future extension du système d'objets.

**Une première trace de l'inconnu du sanctuaire (suite immédiate)** : 2 nouvelles étapes (`identity_search_started` → `identity_hint_gathered`, ce dernier remplaçant `antagonist_glimpsed` comme état terminal et récupérant le statut dédié « Acte 3 en cours »). Purement narrative, aucun nouveau lieu ni combat : Sélène, incapable d'identifier la silhouette entraperçue à la chambre du scellement par ses propres moyens, renvoie le joueur vers Yenn à Vasenoire (`VasenoireScene`, déjà bâtie) — une question neuve pour ce personnage, distincte du fil des Chercheurs d'éclats qu'elle avait déjà refermé plus tôt dans l'histoire (`rival_hunters_confirmed`). Même schéma que les précédents relais narratifs vers un PNJ déjà existant (`outpost_corruption_lead`→`corruption_confirmed`) : récompense versée sur place par Yenn, pas au retour chez Sélène. Récompense à l'étape finale : 240 XP + Gants de cuir (Rare).

**Treizième donjon — la vieille vigie de la confrérie fondatrice (suite immédiate)** : 3 nouvelles étapes (`upstream_lead` → `watchtower_reached` → `watchtower_cleared`, ce dernier remplaçant `identity_hint_gathered` comme état terminal et récupérant le statut dédié « Acte 3 en cours »). Vasenoire (`VasenoireScene`, déjà bâtie) gagne un nouveau passage discret à l'est, toujours praticable plutôt que conditionné au dialogue (même logique que l'autel du Sanctuaire ou l'entrée cachée du Quai clandestin), menant à `SilentWatchScene` — même gabarit "moyen" que les 12 donjons précédents, palette gris-bleu distincte pour marquer un lieu différent. Réutilise `corrupted_sentinel` (déjà l'identité "sentinelle de pierre" du jeu) pour les gardiens plutôt qu'un nouveau monstre d'ambiance. Nouveau boss, `watchtower_guardian`, un gardien de la confrérie qui ne distingue plus l'ami de l'ennemi après des siècles d'abandon. Butin signature dédié : `watchtower_helm` (Heaume de la Vigie oubliée), premier objet signature à occuper l'emplacement casque. Le débriefing confirme que la silhouette du sanctuaire est passée par ce même lieu avant le joueur, sans trancher si elle a affronté le gardien ou l'a évité — la piste continue sans se refermer. Récompense à l'étape finale : 380 XP + Bottes de cuir (Épique).

**Le heaume de la vigie portait un serment (suite immédiate)** : nouvelle étape terminale `helm_inscription_studied`, qui remplace `watchtower_cleared`. Purement narrative, aucun nouveau lieu ni combat : Sélène termine l'examen du heaume rapporté de la vieille vigie et y découvre une inscription — un serment de la confrérie fondatrice, pas un simple mot d'ordre militaire — qui nuance sans la trancher la question de savoir si la silhouette aperçue au sanctuaire est hostile. Cohérent avec le choix déjà noté dans `VISION.md`/`DESIGN.md` de ne pas faire de cet antagoniste un simple méchant caricatural. Le statut dédié dans `QuestLogScene` passe de `watchtower_cleared` à `helm_inscription_studied`, toujours « Acte 3 en cours ». Récompense à l'étape finale : 260 XP + Gants de cuir (Épique).

**Quatorzième donjon — ce que le réseau de vigies protégeait vraiment (suite immédiate)** : 3 nouvelles étapes (`ward_core_lead` → `ward_core_reached` → `ward_core_cleared`, ce dernier remplaçant `helm_inscription_studied` comme état terminal et récupérant le statut dédié « Acte 3 en cours »). La Vigie silencieuse (`SilentWatchScene`, déjà bâtie) gagne un second passage discret, un escalier vers une chambre plus profonde, menant à `WardCoreScene` — même gabarit "moyen" que les 13 donjons précédents, palette froide distincte des deux précédentes. Réutilise `brotherhood_specter` (déjà l'identité "esprits de la confrérie" du jeu, introduite à la Chambre du Scellement) pour les gardiens plutôt qu'un nouveau monstre d'ambiance. Nouveau boss, `unnamed_vestige`, ce que le réseau de vigies de la confrérie fondatrice surveillait réellement — explicitement distingué du Roi Démon dans le débriefing, pour ne pas anticiper sur le climax de l'Acte 3 à venir. Butin signature dédié : `eternal_watch_greaves` (Grèves de la Veille éternelle), premier objet signature à occuper l'emplacement jambes. Récompense à l'étape finale : 410 XP + Bouclier en bois (Épique).

**Un vieux dicton de gardien, transmis d'ermite en ermite (suite immédiate)** : 2 nouvelles étapes (`hermit_lead` → `hermit_confided`, ce dernier remplaçant `ward_core_cleared` comme état terminal et récupérant le statut dédié « Acte 3 en cours »). Purement narrative, aucun nouveau lieu ni combat : Sélène renvoie le joueur vers Aldric, l'ermite du petit sanctuaire (`ShrineScene`, personnage de l'Acte 1) qui n'était jusqu'ici jamais intervenu dans la trame principale malgré son sanctuaire assis sur la chambre du scellement depuis le début du jeu — reprend et paye un personnage ancien plutôt que d'en introduire un nouveau. Même schéma que les précédents relais narratifs vers un PNJ déjà existant (`identity_search_started`→`identity_hint_gathered`) : récompense versée sur place par Aldric, pas au retour chez Sélène (bien que City reçoive tout de même une courte ligne de clôture). Récompense à l'étape finale : 280 XP + Amulette simple (Épique).

**Quinzième donjon — l'Ordre des Veilleurs obtient enfin un nom (suite immédiate)** : 3 nouvelles étapes (`watchers_vault_lead` → `watchers_vault_reached` → `watchers_vault_cleared`, ce dernier remplaçant `hermit_confided` comme état terminal et récupérant le statut dédié « Acte 3 en cours »). Les Archives d'Aiglemont (`ArchivesScene`, personnage/lieu de l'Acte 1, jusqu'ici un simple détour optionnel sans lien avec la trame principale) gagnent un passage caché vers `WatchersVaultScene` — même gabarit "moyen" que les 14 donjons précédents, palette parcheminée distincte des dungeons précédents pour marquer un lieu construit plutôt que naturel ou ruiné. Réutilise `archive_wisp` (déjà l'identité "feu-follet des archives" du jeu) pour les gardiens plutôt qu'un nouveau monstre d'ambiance. Nouveau boss, `last_watcher`, le dernier gardien connu de l'ordre — celui qui n'a jamais douté. Butin signature dédié : `last_watcher_boots` (Bottes du Dernier Veilleur), premier objet signature à occuper l'emplacement bottes. Le débriefing nomme enfin "l'Ordre des Veilleurs" comme tel, tout en soulignant qu'aucun texte public d'Aiglemont n'en garde trace — la trame continue de poser plus de questions qu'elle n'en résout, cohérent avec le rythme tenu depuis le début de l'Acte 3. Récompense à l'étape finale : 440 XP + Casque de cuir (Épique).

**Un message laissé sans témoin, dans la tour de Sélène elle-même (suite immédiate)** : nouvelle étape terminale `silhouette_message_found`, qui remplace `watchers_vault_cleared`. Purement narrative, aucun nouveau lieu ni combat : la silhouette entre en contact direct pour la première fois, un message non signé laissé dans une pièce que Sélène verrouille toujours — la marque du cercle brisé fait écho à celle du heaume de la vigie. Le message recadre la question posée depuis plusieurs incréments ("ce n'est pas qui elle est qui compte — c'est ce qui se réveille"), sans lever l'ambiguïté sur ses intentions, cohérent avec le choix déjà noté dans `VISION.md`/`DESIGN.md` de ne pas en faire un simple méchant caricatural. Le statut dédié dans `QuestLogScene` passe de `watchers_vault_cleared` à `silhouette_message_found`, toujours « Acte 3 en cours ». Récompense à l'étape finale : 300 XP + Anneau simple (Épique).

**Seizième donjon — ce que le vol de l'éclat a réellement dérangé (suite immédiate)** : 3 nouvelles étapes (`tomb_depths_lead` → `tomb_depths_reached` → `tomb_depths_cleared`, ce dernier remplaçant `silhouette_message_found` comme état terminal et récupérant le statut dédié « Acte 3 en cours »). Le tombeau de la confrérie fondatrice (`BrotherhoodTombScene`, climax de l'Acte 2, jamais fouillé au-delà de sa chambre funéraire) gagne une fissure cachée vers `BrokenSleepScene` — même gabarit "moyen" que les 15 donjons précédents, palette rouge sombre distincte pour marquer un réveil plutôt qu'une simple ruine. Réutilise `corrupted_knight` (déjà l'identité "gardiens de la confrérie" de ce donjon) pour les gardiens, désormais "réveillés" plutôt qu'endormis. Nouveau boss, `broken_sleeper`, ce que le vol de l'éclat majeur a réellement dérangé — pas seulement libéré l'émissaire du Roi Démon en surface, mais troublé quelque chose de bien plus ancien en profondeur. Butin signature dédié : `broken_sleep_aegis` (Égide du Sommeil brisé), dernier emplacement d'équipement encore sans objet signature (bouclier), complétant ainsi la couverture de tous les emplacements. Le débriefing confirme que la silhouette savait déjà, avant même le joueur, ce qui se réveillait. Récompense à l'étape finale : 470 XP + Plastron de cuir (Épique).

**Sélène recoupe enfin toute la piste de l'Acte 3 (suite immédiate)** : nouvelle étape terminale `grand_theory_formed`, qui remplace `tomb_depths_cleared`. Purement narrative, aucun nouveau lieu ni combat : Sélène relie les fils accumulés depuis le début de l'Acte 3 (le dicton d'Aldric, l'Ordre des Veilleurs, le message de la silhouette, ce qui dormait sous le tombeau) en une théorie de travail — une chaîne d'événements plutôt qu'un incident isolé — sans encore révéler l'identité de la silhouette ni la nature exacte de la menace, cohérent avec le rythme tenu depuis le début de l'acte. Le statut dédié dans `QuestLogScene` passe de `tomb_depths_cleared` à `grand_theory_formed`, toujours « Acte 3 en cours ». Récompense à l'étape finale : 320 XP + Amulette simple (Rare).

**Dix-septième donjon — ce que le cœur de la corruption n'était qu'un symptôme de (suite immédiate)** : 3 nouvelles étapes (`grove_depths_lead` → `grove_depths_reached` → `grove_depths_cleared`, ce dernier remplaçant `grand_theory_formed` comme état terminal et récupérant le statut dédié « Acte 3 en cours »). Le bosquet corrompu (`BlightedGroveScene`, dixième donjon du jeu, dont le boss avait été présenté comme "une simple retombée locale, pas la source véritable") gagne un passage caché vers `CorruptedRootScene` — même gabarit "moyen" que les 16 donjons précédents, palette verte malsaine distincte du bosquet en surface. Réutilise `blight_spawn` (déjà l'identité "rejeton corrompu" de ce donjon) pour les gardiens. Nouveau boss, `blight_root`, la racine-mère que `corruption_heart` n'était qu'un symptôme de — troisième maillon confirmé de la chaîne théorisée par Sélène, après le tombeau et la vigie. Butin signature dédié : `corrupted_root_gloves` (Gants de la Racine-mère), dernier emplacement d'équipement encore sans objet signature — tous les emplacements du jeu ont désormais au moins un objet signature associé. Récompense à l'étape finale : 500 XP + Jambières de cuir (Épique).

**Sélène formule une hypothèse sur l'identité de la silhouette (suite immédiate)** : nouvelle étape terminale `watcher_hypothesis_formed`, qui remplace `grove_depths_cleared`. Purement narrative, aucun nouveau lieu ni combat : en recoupant tous les indices accumulés (la marque du cercle brisé, la connaissance du serment, l'avance systématique sur chaque maillon de la chaîne), Sélène formule l'hypothèse que la silhouette n'enquête pas sur l'Ordre des Veilleurs mais EN FAIT PARTIE — la dernière encore fidèle au serment. Reste une hypothèse, pas une confirmation : ni nom, ni visage, ni certitude, cohérent avec le choix déjà noté dans `VISION.md`/`DESIGN.md` de ne pas en faire un simple méchant caricatural. Le statut dédié dans `QuestLogScene` passe de `grove_depths_cleared` à `watcher_hypothesis_formed`, toujours « Acte 3 en cours ». Récompense à l'étape finale : 350 XP + Gants de cuir (Épique).

**Dix-huitième donjon — la source de toute la chaîne (suite immédiate)** : 3 nouvelles étapes (`seal_depths_lead` → `seal_depths_reached` → `seal_depths_cleared`, ce dernier remplaçant `watcher_hypothesis_formed` comme état terminal et récupérant le statut dédié « Acte 3 en cours »). La chambre du Scellement (`SealChamberScene`, douzième donjon du jeu, déjà nettoyée de son gardien primordial) gagne un passage caché vers `SealDepthsScene` — même gabarit "moyen" que les 17 donjons précédents, palette blanc-bleu distincte pour marquer la source plutôt qu'un symptôme de plus. Réutilise `corrupted_sentinel` pour les gardiens. Nouveau boss, `seal_echo`, explicitement pas le Roi Démon (le débriefing le précise noir sur blanc) — seulement la contrainte du sceau elle-même qui cède un peu plus, à la source de toute la chaîne théorisée par Sélène. Comme `demon_envoy` en son temps, ne porte aucune récompense signature : tous les emplacements d'équipement en ont déjà une, et cette rencontre reste volontairement un symptôme de plus, pas une conclusion. Le débriefing élargit la portée de la menace : ce n'est plus une chaîne isolée de trois maillons, mais le sceau tout entier qui vacille. Récompense à l'étape finale : 530 XP + Amulette simple (Épique).

**La silhouette approche le joueur directement pour la première fois (suite immédiate)** : nouvelle étape terminale `second_token_found`, qui remplace `seal_depths_cleared`. Purement narrative, aucun nouveau lieu ni combat : un fragment de pierre gravé du même cercle brisé apparaît directement dans les affaires du joueur, pas dans la tour de Sélène cette fois — la silhouette s'est approchée du joueur lui-même. Le fragment porte un seul mot gravé, « Ensemble », que Sélène ne sait pas interpréter comme une main tendue ou un avertissement de plus, cohérent avec le choix déjà noté dans `VISION.md`/`DESIGN.md` de ne pas en faire un simple méchant caricatural. Le statut dédié dans `QuestLogScene` passe de `seal_depths_cleared` à `second_token_found`, toujours « Acte 3 en cours ». Récompense à l'étape finale : 380 XP + Bottes de cuir (Rare).

**Dix-neuvième donjon — la Loge des Veilleurs, et la première rencontre en personne (suite immédiate)** : 3 nouvelles étapes (`lodge_lead` → `lodge_reached` → `lodge_cleared`, ce dernier remplaçant `second_token_found` comme état terminal et récupérant le statut dédié « Acte 3 en cours »). Le petit sanctuaire (`ShrineScene`, lieu de l'Acte 1) gagne un second passage caché, à la pierre extérieure de son propre jardin qu'Aldric n'avait jamais su déchiffrer, menant à `WatchersLodgeScene` — même gabarit "moyen" que les 18 donjons précédents, palette chaude distincte pour marquer un lieu qui n'a jamais été hostile, seulement oublié. Nouveau monstre d'escorte, `watcher_echo` (échos plus tardifs de l'ordre, distincts des `brotherhood_specter` de la confrérie fondatrice), et nouveau boss, `oath_guardian`, une épreuve plutôt qu'un ennemi au sens strict. Comme `seal_echo`, aucune récompense signature. Le débriefing marque la première rencontre en personne entre le joueur et la silhouette — sans hostilité, sans nom donné, mais un visage désormais. Récompense à l'étape finale : 560 XP + Anneau simple (Épique).

**De la compréhension à la réparation (suite immédiate)** : nouvelle étape terminale `reinforcement_plan_started`, qui remplace `lodge_cleared`. Purement narrative, aucun nouveau lieu ni combat : après la première rencontre en personne avec la silhouette, Sélène recentre l'objectif de l'Acte 3 — il ne s'agit plus seulement de comprendre pourquoi le sceau vacille, mais de chercher comment le réparer, une tâche qu'elle estime trop vaste pour une seule personne, faisant écho au mot « Ensemble » laissé par la silhouette. Le statut dédié dans `QuestLogScene` passe de `lodge_cleared` à `reinforcement_plan_started`, toujours « Acte 3 en cours ». Récompense à l'étape finale : 400 XP + Amulette simple (Rare).

**Vingtième donjon — les Archives du Rite (suite immédiate)** : 3 nouvelles étapes (`rite_archive_lead` → `rite_archive_reached` → `rite_archive_cleared`, ce dernier remplaçant `reinforcement_plan_started` comme état terminal et récupérant le statut dédié « Acte 3 en cours »). La Loge des Veilleurs (`WatchersLodgeScene`, dix-neuvième donjon du jeu) gagne un second passage caché, plus profond que la table ronde où le joueur a rencontré la silhouette, menant à `RiteArchiveScene` — même gabarit "moyen" que les 19 donjons précédents, palette parcheminée chaude cousine de la Voûte des Veilleurs. Réutilise `watcher_echo` pour les gardiens. Nouveau boss, `rite_guardian`, posté devant ce que l'Ordre a laissé de plus précieux : les instructions du rite de scellement original, partiellement récupérées. Le débriefing révèle que le rite exigeait plusieurs mains à plusieurs endroits simultanément — jamais l'œuvre d'une seule personne — posant les bases d'une future collaboration explicite avec la silhouette plutôt que de la résoudre dans cet incrément. Récompense à l'étape finale : 590 XP + Plastron de cuir (Épique).

**Le joueur répond à la silhouette pour la première fois (suite immédiate)** : nouvelle étape terminale `response_sent`, qui remplace `rite_archive_cleared`. Purement narrative, aucun nouveau lieu ni combat : Sélène propose de répondre au mot « Ensemble » avec un message en retour, laissé au petit sanctuaire (`ShrineScene`) — geste symétrique au premier contact de la silhouette. Une réponse arrive dès le lendemain, un seul mot : « Bientôt. » Cohérent avec le choix déjà noté dans `VISION.md`/`DESIGN.md` de construire cette relation progressivement plutôt que de la résoudre d'un coup. Le statut dédié dans `QuestLogScene` passe de `rite_archive_cleared` à `response_sent`, toujours « Acte 3 en cours ». Récompense à l'étape finale : 420 XP + Bottes de cuir (Épique).

**Vingt-et-unième donjon — l'Annexe scellée (suite immédiate)** : 3 nouvelles étapes (`rite_annex_lead` → `rite_annex_reached` → `rite_annex_cleared`, ce dernier remplaçant `response_sent` comme état terminal et récupérant le statut dédié « Acte 3 en cours »). Un second message arrive de nuit, en complément du premier « Bientôt » : les rayonnages du fond des Archives du Rite, jusque-là fermés par un ward de l'Ordre lui-même, cèdent enfin — la confiance rendue à la silhouette semble en être la clé. `RiteArchiveScene` (vingtième donjon) gagne son propre passage caché, menant à `RiteAnnexScene` — même gabarit "moyen" que les 20 donjons précédents, palette encore plus sombre, jamais éclairée depuis trois siècles. Réutilise `watcher_echo` pour les gardiens. Nouveau boss, `veiled_scribe`, posté devant le registre complet des membres de l'Ordre — noms et marques, certains très anciens, d'autres bien plus récents : la première confirmation concrète que l'Ordre des Veilleurs n'a jamais vraiment disparu, contrairement à ce que tout le monde supposait jusqu'ici. Pas de récompense signature (les 8 emplacements d'équipement sont déjà tous couverts depuis `corrupted_root_gloves`) : comme pour `seal_echo`, `oath_guardian` et `rite_guardian`, ce boss n'en a pas — encore un jalon de l'enquête, pas une conclusion. Récompense à l'étape finale : 610 XP + Amulette simple (Épique).

**Une lignée se dessine dans le registre de l'Ordre (suite immédiate)** : nouvelle étape terminale `lineage_traced`, qui remplace `rite_annex_cleared`. Purement narrative, aucun nouveau lieu ni combat : Sélène prend quelques jours pour croiser chaque nom du registre récupéré avec ce qui a déjà été découvert (la Loge, le Sommeil brisé, la Voûte des Veilleurs) et en tire un premier résultat concret — un même nom, ou du moins un même sang, revient sur plusieurs générations séparées de décennies. Ne révèle ni l'identité de la silhouette ni le nom lui-même, seulement l'existence d'une filiation : un jalon supplémentaire dans l'enquête, pas une résolution. Le statut dédié dans `QuestLogScene` passe de `rite_annex_cleared` à `lineage_traced`, toujours « Acte 3 en cours ». Récompense à l'étape finale : 340 XP + Anneau simple (Rare).

**Vingt-deuxième donjon — la Crypte des Aînés (suite immédiate)** : 3 nouvelles étapes (`crypt_lead` → `crypt_reached` → `crypt_cleared`, ce dernier remplaçant `lineage_traced` comme état terminal et récupérant le statut dédié « Acte 3 en cours »). Le nom relevé dans le registre de l'Ordre correspond à un acte de propriété jamais mis à jour depuis des générations : une maison scellée dans le vieux quartier d'Aiglemont. `CityScene` gagne sa propre ruelle oubliée, toujours franchissable, menant à `AncestralCryptScene` — même gabarit "moyen" que les 21 donjons précédents, palette pierre froide plutôt que parcheminée (un caveau, pas une bibliothèque). Réutilise `corrupted_knight` pour les gardiens (déjà le "chevalier tombé" générique du jeu). Nouveau boss, `blood_keeper`, posté devant un blason familial gravé sur chaque tombe — le même symbole que celui déjà relevé sur le fragment de la Loge et les pages de l'Archive : la première preuve tangible que cette famille a fait partie de l'Ordre, pas seulement croisé son chemin. Pas de récompense signature (comme pour `seal_echo`, `oath_guardian`, `rite_guardian` et `veiled_scribe`) : encore un jalon, pas une conclusion. Récompense à l'étape finale : 680 XP + Casque de cuir (Épique).

**Un prénom sorti d'une légende (suite immédiate)** : nouvelle étape terminale `first_name_given`, qui remplace `crypt_cleared`. Purement narrative, aucun nouveau lieu ni combat : le joueur porte le blason trouvé dans la crypte au petit sanctuaire, geste désormais rituel. La réponse qui arrive le lendemain rompt avec les précédentes — plus longue, l'écriture moins assurée — et livre un prénom que Sélène reconnaît d'une vieille légende locale qu'elle croyait n'être qu'une légende. Ne révèle ni nom de famille ni identité complète, seulement de quoi inquiéter Sélène elle-même : encore un jalon, pas une résolution. Le statut dédié dans `QuestLogScene` passe de `crypt_cleared` à `first_name_given`, toujours « Acte 3 en cours ». Récompense à l'étape finale : 370 XP + Gants de cuir (Épique).

10. Polish (effets, son, UI) + test offline complet — les effets sonores sont désormais en place (voir ci-dessus) ; il reste à intégrer de vrais assets graphiques et de la musique (voir section Assets) à la place des rectangles de couleur actuels, volontairement hors scope v1.

## Assets

Pas de pack d'art fantasy pour l'instant — tout le rendu (perso, bâtiments, monstres, donjon) est en formes géométriques de couleur en attendant. Le joueur fournira des packs gratuits une fois qu'on attaque le graphisme (prévu à l'incrément 10, polish) : LPC Base Assets, 0x72 DungeonTileset II (+ Extended), Kenney Tiny Dungeon, FreePixel.art RPG, Pixel Fantasy Icons, Ansimuz Legacy, Kenney Mini Dungeon — à intégrer dans cet ordre de priorité. Icônes PWA actuelles = placeholders générés par `scripts/generate-icons.mjs`, à remplacer en même temps.

**Génération d'assets via SpriteCook** : en complément des packs gratuits ci-dessus, un pipeline de génération IA (service SpriteCook, connecté en MCP) permet de produire des sprites sur mesure pour les identités du jeu qu'aucun pack existant ne couvre exactement (30 monstres nommés dans `monster.ts`). Style verrouillé pour toutes les générations : pixel art 16-bit, palette terreuse (cuir/pierre/fer), proportions réalistes (pas de chibi), fond transparent, en référence visuelle sur le héros/rat/squelette du mini pack déjà intégré. Inventaire généré à ce jour (voir `asset/spritecook-manifest.json` pour les identifiants stables) : `corrupted_wolf`, `goblin_scout`, `cave_spider`, `rat_king`, `bandit_thug`, `goblin_brute` (Tier 1 — région de départ, en cours). Limitation technique actuelle : l'environnement d'exécution ne peut pas télécharger lui-même les fichiers depuis SpriteCook (accès réseau direct à l'hôte non autorisé) — les PNG doivent être récupérés via l'appli/le lien signé puis réintégrés manuellement au dépôt, comme pour le mini pack initial. Seuls les identifiants et prompts sont donc versionnés pour l'instant, pas encore les fichiers image eux-mêmes.
